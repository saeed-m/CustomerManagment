<UserControl x:Class="CustomerManagment.WpfApp.Controls.StatusComboBox"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CustomerManagment.WpfApp.Controls"
             xmlns:models="clr-namespace:CustomerManagment.WpfApp.Data.Models"
             mc:Ignorable="d" 
             d:DesignHeight="40" d:DesignWidth="200">

    <ComboBox x:Name="cbStatus" 
              SelectedValue="{Binding SelectedStatus, RelativeSource={RelativeSource AncestorType=UserControl}}"
              SelectedValuePath="Status">
        <ComboBox.ItemTemplate>
            <DataTemplate>
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{Binding Icon}" Margin="0,0,5,0"/>
                    <TextBlock Text="{Binding DisplayName}"/>
                </StackPanel>
            </DataTemplate>
        </ComboBox.ItemTemplate>
    </ComboBox>
</UserControl>